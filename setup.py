from setuptools import setup, find_packages

setup(
    name='sampleproject',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Your Name',
    description='A sample Python project for documentation demo',
) 